package com.example.webworks.neostore.addaddress

import android.os.Bundle
import android.support.v7.app.AppCompatActivity
import android.support.v7.widget.LinearLayoutManager
import com.example.webworks.neostore.R
import kotlinx.android.synthetic.main.recycler_view.*

class AddressList : AppCompatActivity(){
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.address_list_activity)
/*
        val address=intent.getStringExtra("address")
        val landmark=intent.getStringExtra("landmark")
        val city=intent.getStringExtra("city")
        val state=intent.getStringExtra("state")
        val zipCode=intent.getStringExtra("zip_code")
        val country=intent.getStringExtra("country")

        val allAddress :ArrayList<String> = ArrayList()
        allAddress.add(address)
        allAddress.add(landmark)
        allAddress.add(city)
        allAddress.add(state)
        allAddress.add(zipCode)
        allAddress.add(country)
        AddListAdapter(allAddress)*/





    }

}